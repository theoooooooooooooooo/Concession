{% extends 'base.html.twig' %}

{% block title %}Modife Pièce{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h2 class="text-center mb-4">Modife Pièce</h2>
        <div class="form-container">
            {{ include('piece/_form.html.twig') }}
                <a href="{{ path('app_piece_index') }}" class="custom-btn">Retour</a>
                {{ include('piece/_delete_form.html.twig') }}
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
