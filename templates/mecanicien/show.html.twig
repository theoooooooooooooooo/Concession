{% extends 'base.html.twig' %}

{% block title %}Mecanicien{% endblock %}

{% block body %}
    <h1>Mecanicien</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mecanicien.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ mecanicien.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ mecanicien.prenom }}</td>
            </tr>
            <tr>
                <th>Speciamiste</th>
                <td>{{ mecanicien.speciamiste }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mecanicien_index') }}">back to list</a>

    <a href="{{ path('app_mecanicien_edit', {'id': mecanicien.id}) }}">edit</a>

    {{ include('mecanicien/_delete_form.html.twig') }}
{% endblock %}
