{% extends 'base.html.twig' %}

{% block title %}Nouvelle vente{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h2 class="text-center mb-4">Nouvelle Vente</h2>
        <div class="form-container">
            {{ include('vente/_form.html.twig') }}
                <a href="{{ path('app_vente_index') }}" class="custom-btn">Retour</a>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
