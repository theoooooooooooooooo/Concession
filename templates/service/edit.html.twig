{% extends 'base.html.twig' %}

{% block title %}Modife Service{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h2 class="text-center mb-4">Modife Service</h2>
        <div class="form-container">
            {{ include('service/_form.html.twig') }}
                <a href="{{ path('app_service_index') }}" class="custom-btn">Retour</a>
                {{ include('service/_delete_form.html.twig') }}
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
