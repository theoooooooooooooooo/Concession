<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Concession Auto{% endblock %}</title>

        <!-- Bootstrap CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

        <!-- Custom CSS -->
        <link href="{{ asset('css/style.css') }}" rel="stylesheet">
        <link href="{{ asset('css/form.css') }}" rel="stylesheet">
        <link href="{{ asset('css/table.css') }}" rel="stylesheet">
        <style>
            /* Style du bouton personnalisé */
            .custom-btn {
                display: inline-block;
                background-color: #343a40; /* Couleur de fond sombre */
                color: white; /* Texte en blanc */
                padding: 0.6rem 1.2rem;
                text-align: center;
                font-size: 1rem;
                border-radius: 4px;
                text-decoration: none; /* Enlever le soulignement */
                transition: background-color 0.3s, transform 0.3s;
            }

            /* Effet au survol */
            .custom-btn:hover {
                background-color: #ffc107; /* Changement de couleur au survol */
                transform: translateY(-2px); /* Légère élévation au survol */
            }

            /* Optionnel : Effet focus pour l'accessibilité */
            .custom-btn:focus {
                outline: none;
                box-shadow: 0 0 0 2px rgba(255, 255, 255, 0.6); /* Ombre blanche au focus */
            }
        </style>

        {% block stylesheets %}
        {% endblock %}
    </head>
    <body class="bg-light">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container">
                <a class="navbar-brand" href="#">Concession Auto</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_vehicule_index') }}">Véhicules</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_categorie_index') }}">Catégories</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_client_index') }}">Client</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_vente_index') }}">Vente</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_service_index') }}">Service Après Vente</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_mecanicien_index') }}">Mecanicien</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_piece_index') }}">Pièce</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
        <main class="container mt-4">
            {% block body %}{% endblock %}
        </main>

        <!-- Bootstrap JS -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    </body>
</html>
