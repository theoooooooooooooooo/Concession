{% extends 'base.html.twig' %}

{% block title %}Nouveau Categorie{% endblock %}

{% block body %}
    <div class="container mt-5">
        <h2 class="text-center mb-4">Nouveau Categorie</h2>
        <div class="form-container">
            {{ include('categorie/_form.html.twig') }}
                <a href="{{ path('app_categorie_index') }}" class="custom-btn">Retour</a>
            </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
